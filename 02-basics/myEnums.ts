enum SeatChoice {
  Window="10",
  Middle=1,
  Aisle,
  None,
}
//we can change the value of the variable inside enum
//the variable value will automatically calculated
let hcSeat= SeatChoice.Aisle;
console.log(hcSeat);
